
/*  ctrl  +`  ctrl+shift+a
    concat()生成新数组，
    entries()返回一个新的数组遍历对象，该对象包含数组中每个索引的键值对
    every()
    参数:函数
    返回值:布尔值
    作用：测试一个数组内的所有元素是否都能通过某个指定函数的测试
 */
var obj = {name:'zz',age:20}
var arr1 = ['a','b','c']
var arr2 = [1,4,6,9]
// var arr3 = arr.concat(3)

// const iterator1 = arr.entries();
// console.log(iterator1.next().value);

// var a = arr2.every((item)=>item<10)
// console.log(a);
var arr3 = []
 var a = arr3.fill(0,10,2)
 console.log(a);